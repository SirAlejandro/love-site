import React from "react";
import ReactDOM from "react-dom/client";
import LoveSite from "./LoveSite.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LoveSite />
  </React.StrictMode>
);
